name: Publish Release
"on":
  release:
jobs:
  PublishPackages:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    permissions:
      contents: write
    steps:
      - name: Test
        run: echo "${{ github.event.action }}"
